---
version: 1
---

# Transient

Project-specific transient scratch. See `^/^/.tmp/start.md` for the full charter.

Non-authoritative and git-ignored (only this `start.md` is tracked). Scratch defaults here per the transient-gravity rule (`^/^/.codex/implicit/02-foundational/transient-gravity.md`) — write throwaway artifacts to this project's own `.tmp/`, not the parent's. Purge-eligible.
